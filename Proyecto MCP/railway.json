{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
